"""Miner tooling: the CLI and the reference template. architecture.md 25.

A miner does not run a server. The laboratory is a container the validator executes, so this package
is a build-and-submit toolchain rather than a neuron.
"""
